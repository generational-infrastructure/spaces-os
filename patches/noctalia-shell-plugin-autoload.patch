--- a/Services/Noctalia/PluginRegistry.qml	2026-05-20 06:33:18.596641920 +0000
+++ b/Services/Noctalia/PluginRegistry.qml	2026-05-20 06:33:24.142784896 +0000
@@ -305,7 +305,13 @@
       }
 
       Logger.i("PluginRegistry", "All plugin manifests loaded. Total plugins:", loadedCount);
-      root.pluginsChanged();
+      // Hand `pluginsChanged` off to the autoload manager so it can
+      // synchronously GC stale autoload-owned entries from pluginStates
+      // before PluginService.init() observes them — otherwise ghosts
+      // trigger marketplace install attempts and surface as toast errors.
+      PluginAutoload.processAutoloadDir(function () {
+        root.pluginsChanged();
+      });
       scanProcess.destroy();
     });
   }
--- a/Services/Noctalia/PluginService.qml	2026-05-20 06:33:18.615023246 +0000
+++ b/Services/Noctalia/PluginService.qml	2026-05-20 06:33:32.533988243 +0000
@@ -238,6 +238,7 @@
       // All plugins finished loading
       root.pluginsFullyLoaded = true;
       Logger.i("PluginService", "All plugins loaded");
+      PluginAutoload.addAutoloadedWidgets();
       root.allPluginsLoaded();
 
       // Fetch available plugins from all sources
