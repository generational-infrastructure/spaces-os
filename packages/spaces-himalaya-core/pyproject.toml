[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "spaces-himalaya-core"
version = "0.1.0"
description = "Shared himalaya CLI core for spaces mail-family integrations"
license = "MIT"
requires-python = ">=3.10"

[tool.hatch.build.targets.wheel]
only-include = ["spaces_himalaya_core.py"]
