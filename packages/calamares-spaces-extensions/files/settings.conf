# Spaces OS Calamares config.
#
# Forked from calamares-nixos-extensions/config/settings.conf.
# Differences vs upstream:
#   - `packagechooser` show step removed (spaces always installs niri).
#   - `notesqml@unfree` show step + `instances:` entry removed (was the
#     companion unfree-warning page for packagechooser).
#   - `branding: spaces-os` instead of `nixos` (Spaces OS product name).
# Everything else (modules-search, prompt flags, …) inherited.

modules-search: [ local, @out@/lib/calamares/modules ]

sequence:
- show:
  - welcome
  - locale
  - keyboard
  - users
  - partition
  - summary
- exec:
  - partition
  - mount
  - nixos
  - users
  - umount
- show:
  - finished

branding: spaces-os

prompt-install: false
dont-chroot: false
oem-setup: false
disable-cancel: false
disable-cancel-during-exec: false
hide-back-and-next-during-exec: false
quit-at-end: false
